{
  "name": "taskflow-workspace",
  "version": "1.0.0",
  "description": "Root workspace for TaskFlow application",
  "scripts": {
    "start": "npm start --prefix taskflow/frontend",
    "dev": "npm run dev --prefix taskflow/frontend",
    "frontend": "npm run dev --prefix taskflow/frontend",
    "backend": "cd taskflow/backend && .\\venv\\Scripts\\python -m uvicorn main:app --reload",
    "install:all": "npm install --prefix taskflow/frontend"
  }
}
